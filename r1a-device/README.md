# R1A device harness — reconstructed audit note

This directory is the canonical location for the gadget-side FunctionFS holder harness.

The intended source is:

```text
r1a_ffs_out_v2.c
```

In the expanded evidence epoch, `device_harness` is load-bearing because the negative R1A result depends on the holder witness. The exact original tested source must therefore be imported and hash-pinned; it must not be reconstructed from prose.

At the time this reconstructed audit note was added, the exact canonical source bytes were not yet present in the repository. That absence is a provenance gap, not a runtime result.
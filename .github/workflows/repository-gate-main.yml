name: Repository gate on canonical main

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  repository-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Run fail-closed repository gate
        run: ./VERIFY-REPOSITORY.sh
